{
  "name": "relay-server-and-client",
  "version": "1.0.0",
  "description": "Connect a browser to another browser using SS58 addresses",
  "type": "module",
  "scripts": {
    "start": "vite",
    "start:cloud": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "relay": "node relay.js",
    "test": "npm run build && playwright test test",
    "test:docker": "docker compose run --no-deps test",
    "test:cleanup": "docker container prune -f && docker image prune -f",
    "test:full": "npm run test:docker && npm run test:cleanup",
    "docker:cleanup": "docker compose down --rmi all --volumes --remove-orphans && docker system prune -f"
  },
  "dependencies": {
    "@chainsafe/libp2p-noise": "^16.0.0",
    "@chainsafe/libp2p-yamux": "^7.0.0",
    "@libp2p/circuit-relay-v2": "^3.0.0",
    "@libp2p/identify": "^3.0.1",
    "@libp2p/peer-id": "^3.0.0",
    "@libp2p/ping": "^2.0.1",
    "@libp2p/webrtc": "^5.0.0",
    "@libp2p/websockets": "^9.0.0",
    "@multiformats/multiaddr": "^12.0.0",
    "@polkadot/keyring": "^12.6.2",
    "@polkadot/util": "^12.6.2",
    "it-byte-stream": "^2.0.0",
    "libp2p": "^2.0.0",
    "node-fetch": "^3.3.2",
    "uint8arrays": "^5.0.0",
    "vite": "^6.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "test-ipfs-example": "^1.0.0"
  }
}